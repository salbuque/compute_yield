---
title: "sesitivity_sobel_try"
author: "Simone Albuquerque"
date: "4/21/2021"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(sensitivity)
library(tidyverse)
library(pse)
library(here)
library(gridExtra)
library(purrr)
```

```{r read in function}
# read in function based on your almond yield function that returns the mean and inter-annual variation in yield anomaly.

source("almond_yield_model_SA.R")
```


```{r Latin Hyper Cube SA}
# Perform a  Latin Hypercube (LHR) sensitivity analysis of the 2 coefficients on precipitation - assume that # uniform distribution ("qunif") 
#min value 20% below the default value 
#max value 20% above the default value.
```


```{r Plot Output Change with Parameter Uncertainty}
# Plotting
#1) the scatter plot that shows how output change with parameter uncertainty
```


```{r Plot PRCC}
#2) the plot of the PRCC. 

```

```{r}
# In your Rmarkdown, note which of the two parameters contribute more to parameter uncertainty.
# Submit your Rmarkdown (as a PDF or HTML) with code and the two graphs included

```

